#include "main.h"

/**
 * mul - contain the integers to multiply
 * @a: first number to multiply
 * @b: second integer to multiply
 * return (r)
 */

int mul(int a, int b)
{
	int r;

	r = a * b;

	return (r);
}
